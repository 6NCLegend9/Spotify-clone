export const metadata = {
  title: "Reset Password",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
  referrer: "no-referrer",
};

export default function ResetPasswordTokenLayout({ children }) {
  return children;
}
