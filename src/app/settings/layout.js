export const metadata = {
  title: "Settings",
  description: "Playback, discovery, privacy, and audio preferences.",
  robots: { index: false, follow: false },
};

export default function SettingsLayout({ children }) {
  return children;
}
