export const metadata = {
  title: "Liked Songs",
  description: "Your saved tracks in one place.",
  robots: { index: false, follow: false },
};

export default function LikedSongsLayout({ children }) {
  return children;
}
